<div class="container">
  <div class="container-brandcard mt-5">
    <div class="flex-container-card-category">
      <% @buddies.each do |buddy| %>
        <div class="card-body">
          <h5 class="card-title"><%= buddy.first_name %></h5>
          <p class="card-text"><%= buddy.golf_course.name %></p>
        </div>
      <% end %>
    </div>
  </div>
</div>
